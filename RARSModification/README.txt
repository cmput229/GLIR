To build the modified RARS which supports unicode printing using the printString syscall:

Clone the RARS repository
Navigate to /rars/riscv/syscalls/
Replace the NullString.java in the rars folder with the one found with this README
Build using build-jar.sh